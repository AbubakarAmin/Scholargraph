# `setup.py`

## Responsibility

Legacy bootstrap helper that installs dependencies, creates directories, prepares environment files, and runs compatibility checks.

## Refactor note

Modern project setup should eventually move to a standard packaging and task configuration. Keep this script only while existing users rely on it.
